export class ShippingService {
  shipOrder(address: string): boolean {
    console.log(`🚢 Shipping order to ${address}...`);

    // *** --- your actual logic goes here --- ***

    return true;
  }
}
